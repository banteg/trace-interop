# H02 — trace_get selector and return shape

**Disposition:** Spec decision. Proposal, awaiting client review.

## Proposed behavior

Treat the argument as one traceAddress path: [] is root, [0] first child, [6,0] a nested child. Return one trace object, or null when the transaction/path does not exist. Specify the wire encoding of indices explicitly (prefer hex quantities for compatibility with these requests).

## Rationale

The lookup argument should identify the same path that traceAddress describes, with a singular result matching the method name.

## Open questions

Choose the tree-path contract and align Reth/Nethermind; the positive nested discriminator is now available.

## Evidence

- [get-missing](../../evidence/2026-09-15/cases/get-missing.json)
- [get-missing-tx](../../evidence/2026-09-15/cases/get-missing-tx.json)
- [get-nested](../../evidence/2026-09-15/cases/get-nested.json)
- [get-one](../../evidence/2026-09-15/cases/get-one.json)
- [get-root](../../evidence/2026-09-15/cases/get-root.json)
- [get-transfer-root](../../evidence/2026-09-15/cases/get-transfer-root.json)
- [get-zero](../../evidence/2026-09-15/cases/get-zero.json)
- [semantic-01--get-nested-positive](../../evidence/2026-09-15/cases/semantic-01--get-nested-positive.json)
- [semantic-01--get-nested-parent](../../evidence/2026-09-15/cases/semantic-01--get-nested-parent.json)

## Observations

- go-ethereum_trace: **matches**, `geth-e29edff-a/get-nested-positive` — Return the transaction-tree record at [6, 0], or null if absent.
- go-ethereum_trace: **matches**, `geth-e29edff-a/get-nested-parent` — Return the transaction-tree record at [6], or null if absent.
- go-ethereum_trace: **matches**, `geth-e29edff-initial/get-root` — Return the transaction-tree record at [], or null if absent.
- go-ethereum_trace: **matches**, `geth-e29edff-initial/get-zero` — Return the transaction-tree record at [0], or null if absent.
- go-ethereum_trace: **matches**, `geth-e29edff-initial/get-one` — Return the transaction-tree record at [1], or null if absent.
- go-ethereum_trace: **matches**, `geth-e29edff-initial/get-nested` — Return the transaction-tree record at [0, 0], or null if absent.
- go-ethereum_trace: **matches**, `geth-e29edff-initial/get-transfer-root` — Return one object whose traceAddress equals [].
- besu_development: **matches**, `verified-a/get-nested-positive` — Return the transaction-tree record at [6, 0], or null if absent.
- besu_development: **matches**, `verified-a/get-nested-parent` — Return the transaction-tree record at [6], or null if absent.
- besu_development: **matches**, `verified-initial/get-root` — Return the transaction-tree record at [], or null if absent.
- besu_development: **matches**, `verified-initial/get-zero` — Return the transaction-tree record at [0], or null if absent.
- besu_development: **matches**, `verified-initial/get-one` — Return the transaction-tree record at [1], or null if absent.
- besu_development: **matches**, `verified-initial/get-nested` — Return the transaction-tree record at [0, 0], or null if absent.
- besu_development: **matches**, `verified-initial/get-transfer-root` — Return one object whose traceAddress equals [].
- besu_release: **matches**, `verified-a-besu-retry/get-nested-positive` — Return the transaction-tree record at [6, 0], or null if absent.
- besu_release: **matches**, `verified-a-besu-retry/get-nested-parent` — Return the transaction-tree record at [6], or null if absent.
- besu_release: **matches**, `verified-initial/get-root` — Return the transaction-tree record at [], or null if absent.
- besu_release: **matches**, `verified-initial/get-zero` — Return the transaction-tree record at [0], or null if absent.
- besu_release: **matches**, `verified-initial/get-one` — Return the transaction-tree record at [1], or null if absent.
- besu_release: **matches**, `verified-initial/get-nested` — Return the transaction-tree record at [0, 0], or null if absent.
- besu_release: **matches**, `verified-initial/get-transfer-root` — Return one object whose traceAddress equals [].
- erigon_development: **matches**, `verified-a/get-nested-positive` — Return the transaction-tree record at [6, 0], or null if absent.
- erigon_development: **matches**, `verified-a/get-nested-parent` — Return the transaction-tree record at [6], or null if absent.
- erigon_development: **matches**, `verified-initial/get-root` — Return the transaction-tree record at [], or null if absent.
- erigon_development: **matches**, `verified-initial/get-zero` — Return the transaction-tree record at [0], or null if absent.
- erigon_development: **matches**, `verified-initial/get-one` — Return the transaction-tree record at [1], or null if absent.
- erigon_development: **matches**, `verified-initial/get-nested` — Return the transaction-tree record at [0, 0], or null if absent.
- erigon_development: **matches**, `verified-initial/get-transfer-root` — Return one object whose traceAddress equals [].
- erigon_release: **change_needed**, `verified-a/get-nested-positive` — Return the transaction-tree record at [6, 0], or null if absent.
- erigon_release: **matches**, `verified-a/get-nested-parent` — Return the transaction-tree record at [6], or null if absent.
- erigon_release: **change_needed**, `verified-initial/get-root` — Return the transaction-tree record at [], or null if absent.
- erigon_release: **matches**, `verified-initial/get-zero` — Return the transaction-tree record at [0], or null if absent.
- erigon_release: **matches**, `verified-initial/get-one` — Return the transaction-tree record at [1], or null if absent.
- erigon_release: **matches**, `verified-initial/get-nested` — Return the transaction-tree record at [0, 0], or null if absent.
- erigon_release: **change_needed**, `verified-initial/get-transfer-root` — Return one object whose traceAddress equals [].
- nethermind_development: **change_needed**, `verified-a/get-nested-positive` — Return the transaction-tree record at [6, 0], or null if absent.
- nethermind_development: **change_needed**, `verified-a/get-nested-parent` — Return the transaction-tree record at [6], or null if absent.
- nethermind_development: **change_needed**, `verified-initial/get-root` — Return the transaction-tree record at [], or null if absent.
- nethermind_development: **change_needed**, `verified-initial/get-zero` — Return the transaction-tree record at [0], or null if absent.
- nethermind_development: **change_needed**, `verified-initial/get-one` — Return the transaction-tree record at [1], or null if absent.
- nethermind_development: **change_needed**, `verified-initial/get-nested` — Return the transaction-tree record at [0, 0], or null if absent.
- nethermind_development: **change_needed**, `verified-initial/get-transfer-root` — Return one object whose traceAddress equals [].
- nethermind_release: **change_needed**, `verified-a/get-nested-positive` — Return the transaction-tree record at [6, 0], or null if absent.
- nethermind_release: **change_needed**, `verified-a/get-nested-parent` — Return the transaction-tree record at [6], or null if absent.
- nethermind_release: **change_needed**, `verified-initial/get-root` — Return the transaction-tree record at [], or null if absent.
- nethermind_release: **change_needed**, `verified-initial/get-zero` — Return the transaction-tree record at [0], or null if absent.
- nethermind_release: **change_needed**, `verified-initial/get-one` — Return the transaction-tree record at [1], or null if absent.
- nethermind_release: **change_needed**, `verified-initial/get-nested` — Return the transaction-tree record at [0, 0], or null if absent.
- nethermind_release: **change_needed**, `verified-initial/get-transfer-root` — Return one object whose traceAddress equals [].
- reth_development: **change_needed**, `verified-a/get-nested-positive` — Return the transaction-tree record at [6, 0], or null if absent.
- reth_development: **change_needed**, `verified-a/get-nested-parent` — Return the transaction-tree record at [6], or null if absent.
- reth_development: **change_needed**, `verified-initial/get-root` — Return the transaction-tree record at [], or null if absent.
- reth_development: **change_needed**, `verified-initial/get-zero` — Return the transaction-tree record at [0], or null if absent.
- reth_development: **change_needed**, `verified-initial/get-one` — Return the transaction-tree record at [1], or null if absent.
- reth_development: **matches**, `verified-initial/get-nested` — Return the transaction-tree record at [0, 0], or null if absent.
- reth_development: **change_needed**, `verified-initial/get-transfer-root` — Return one object whose traceAddress equals [].
- reth_release: **change_needed**, `verified-a/get-nested-positive` — Return the transaction-tree record at [6, 0], or null if absent.
- reth_release: **change_needed**, `verified-a/get-nested-parent` — Return the transaction-tree record at [6], or null if absent.
- reth_release: **change_needed**, `verified-initial/get-root` — Return the transaction-tree record at [], or null if absent.
- reth_release: **change_needed**, `verified-initial/get-zero` — Return the transaction-tree record at [0], or null if absent.
- reth_release: **change_needed**, `verified-initial/get-one` — Return the transaction-tree record at [1], or null if absent.
- reth_release: **matches**, `verified-initial/get-nested` — Return the transaction-tree record at [0, 0], or null if absent.
- reth_release: **change_needed**, `verified-initial/get-transfer-root` — Return one object whose traceAddress equals [].
