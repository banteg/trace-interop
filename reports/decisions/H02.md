# H02 — trace_get selector and return shape

**Disposition:** Spec decision. Proposal, awaiting client review.

## Proposed behavior

Treat the argument as one traceAddress path: [] is root, [0] first child, [6,0] a nested child. Return one trace object, or null when the transaction/path does not exist. Specify the wire encoding of indices explicitly (prefer hex quantities for compatibility with these requests).

## Rationale

The lookup argument should identify the same path that traceAddress describes, with a singular result matching the method name.

## September 15 observation

For a simple transfer, [] returns the root object in Erigon/Besu, null in Reth and [] in Nethermind. On the call tree, ["0x0"] returns the first child in Erigon/Besu and the root in Reth. Nethermind returns arrays; ["0x0","0x0"] returns two records rather than a nested-path lookup. A positive ["0x6","0x0"] probe selects the actual nested SELFDESTRUCT in Erigon/Besu. Reth returns null; Nethermind returns two unrelated records at [6] and [0].

## Unresolved review

Choose the tree-path contract and align Reth/Nethermind; the positive nested discriminator is now available.

## Historical evidence

- [get-missing](../../evidence/2026-09-15/cases/get-missing.json)
- [get-missing-tx](../../evidence/2026-09-15/cases/get-missing-tx.json)
- [get-nested](../../evidence/2026-09-15/cases/get-nested.json)
- [get-one](../../evidence/2026-09-15/cases/get-one.json)
- [get-root](../../evidence/2026-09-15/cases/get-root.json)
- [get-transfer-root](../../evidence/2026-09-15/cases/get-transfer-root.json)
- [get-zero](../../evidence/2026-09-15/cases/get-zero.json)
- [semantic-01--get-nested-positive](../../evidence/2026-09-15/cases/semantic-01--get-nested-positive.json)
- [semantic-01--get-nested-parent](../../evidence/2026-09-15/cases/semantic-01--get-nested-parent.json)

## Fresh assertions

- besu_development: **matches**, `initial/get-root` — Return one object whose traceAddress equals [].
- besu_development: **matches**, `initial/get-zero` — Return one object whose traceAddress equals [0].
- besu_development: **matches**, `initial/get-one` — Return one object whose traceAddress equals [1].
- besu_development: **matches**, `initial/get-transfer-root` — Return one object whose traceAddress equals [].
- besu_release: **matches**, `initial/get-root` — Return one object whose traceAddress equals [].
- besu_release: **matches**, `initial/get-zero` — Return one object whose traceAddress equals [0].
- besu_release: **matches**, `initial/get-one` — Return one object whose traceAddress equals [1].
- besu_release: **matches**, `initial/get-transfer-root` — Return one object whose traceAddress equals [].
- erigon_development: **matches**, `initial/get-root` — Return one object whose traceAddress equals [].
- erigon_development: **matches**, `initial/get-zero` — Return one object whose traceAddress equals [0].
- erigon_development: **matches**, `initial/get-one` — Return one object whose traceAddress equals [1].
- erigon_development: **matches**, `initial/get-transfer-root` — Return one object whose traceAddress equals [].
- erigon_release: **change_needed**, `initial/get-root` — Return one object whose traceAddress equals [].
- erigon_release: **matches**, `initial/get-zero` — Return one object whose traceAddress equals [0].
- erigon_release: **matches**, `initial/get-one` — Return one object whose traceAddress equals [1].
- erigon_release: **change_needed**, `initial/get-transfer-root` — Return one object whose traceAddress equals [].
- nethermind_development: **change_needed**, `initial/get-root` — Return one object whose traceAddress equals [].
- nethermind_development: **change_needed**, `initial/get-zero` — Return one object whose traceAddress equals [0].
- nethermind_development: **change_needed**, `initial/get-one` — Return one object whose traceAddress equals [1].
- nethermind_development: **change_needed**, `initial/get-transfer-root` — Return one object whose traceAddress equals [].
- nethermind_release: **change_needed**, `initial/get-root` — Return one object whose traceAddress equals [].
- nethermind_release: **change_needed**, `initial/get-zero` — Return one object whose traceAddress equals [0].
- nethermind_release: **change_needed**, `initial/get-one` — Return one object whose traceAddress equals [1].
- nethermind_release: **change_needed**, `initial/get-transfer-root` — Return one object whose traceAddress equals [].
- reth_development: **change_needed**, `initial/get-root` — Return one object whose traceAddress equals [].
- reth_development: **change_needed**, `initial/get-zero` — Return one object whose traceAddress equals [0].
- reth_development: **change_needed**, `initial/get-one` — Return one object whose traceAddress equals [1].
- reth_development: **change_needed**, `initial/get-transfer-root` — Return one object whose traceAddress equals [].
- reth_release: **change_needed**, `initial/get-root` — Return one object whose traceAddress equals [].
- reth_release: **change_needed**, `initial/get-zero` — Return one object whose traceAddress equals [0].
- reth_release: **change_needed**, `initial/get-one` — Return one object whose traceAddress equals [1].
- reth_release: **change_needed**, `initial/get-transfer-root` — Return one object whose traceAddress equals [].
