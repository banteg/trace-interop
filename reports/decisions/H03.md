# H03 — Filter composition and mode

**Disposition:** Spec decision. Proposal, awaiting client review.

## Proposed behavior

Use OR within each address list and AND between fromAddress and toAddress. One unconstrained side should not suppress matches. If union is retained, expose it explicitly as an optional extension and reject unsupported mode values rather than silently ignoring their meaning.

## Rationale

Supplying a second filter normally narrows a search; combining two sets of alternatives should be explicit.

## Open questions

Settle boolean composition and extension validation together; one-sided and unknown-mode fixtures are available.

## Evidence

- [filter-both](../../evidence/2026-09-15/cases/filter-both.json)
- [filter-from](../../evidence/2026-09-15/cases/filter-from.json)
- [filter-intersection](../../evidence/2026-09-15/cases/filter-intersection.json)
- [filter-to](../../evidence/2026-09-15/cases/filter-to.json)
- [filter-union](../../evidence/2026-09-15/cases/filter-union.json)
- [semantic-01--filter-from-only-intersection](../../evidence/2026-09-15/cases/semantic-01--filter-from-only-intersection.json)
- [semantic-01--filter-to-only-intersection](../../evidence/2026-09-15/cases/semantic-01--filter-to-only-intersection.json)
- [semantic-01--filter-both-unknown-mode](../../evidence/2026-09-15/cases/semantic-01--filter-both-unknown-mode.json)

## Observations

- go-ethereum_trace: **matches**, `geth-final-a/filter-all` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- go-ethereum_trace: **matches**, `geth-final-initial/filter-all` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- go-ethereum_trace: **matches**, `geth-final-initial/filter-from` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- go-ethereum_trace: **matches**, `geth-final-initial/filter-to` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- go-ethereum_trace: **matches**, `geth-final-initial/filter-both` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- besu_development: **matches**, `verified-a/filter-all` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- besu_development: **matches**, `verified-initial/filter-all` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- besu_development: **matches**, `verified-initial/filter-from` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- besu_development: **change_needed**, `verified-initial/filter-to` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- besu_development: **matches**, `verified-initial/filter-both` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- besu_release: **matches**, `verified-a-besu-retry/filter-all` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- besu_release: **matches**, `verified-initial/filter-all` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- besu_release: **matches**, `verified-initial/filter-from` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- besu_release: **change_needed**, `verified-initial/filter-to` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- besu_release: **matches**, `verified-initial/filter-both` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- erigon_development: **matches**, `verified-a/filter-all` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- erigon_development: **matches**, `verified-initial/filter-all` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- erigon_development: **matches**, `verified-initial/filter-from` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- erigon_development: **matches**, `verified-initial/filter-to` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- erigon_development: **change_needed**, `verified-initial/filter-both` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- erigon_release: **matches**, `verified-a/filter-all` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- erigon_release: **matches**, `verified-initial/filter-all` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- erigon_release: **matches**, `verified-initial/filter-from` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- erigon_release: **matches**, `verified-initial/filter-to` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- erigon_release: **change_needed**, `verified-initial/filter-both` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- nethermind_development: **matches**, `verified-a/filter-all` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- nethermind_development: **matches**, `verified-initial/filter-all` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- nethermind_development: **matches**, `verified-initial/filter-from` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- nethermind_development: **matches**, `verified-initial/filter-to` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- nethermind_development: **matches**, `verified-initial/filter-both` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- nethermind_release: **matches**, `verified-a/filter-all` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- nethermind_release: **matches**, `verified-initial/filter-all` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- nethermind_release: **matches**, `verified-initial/filter-from` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- nethermind_release: **matches**, `verified-initial/filter-to` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- nethermind_release: **matches**, `verified-initial/filter-both` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- reth_development: **matches**, `verified-a/filter-all` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- reth_development: **matches**, `verified-initial/filter-all` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- reth_development: **matches**, `verified-initial/filter-from` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- reth_development: **matches**, `verified-initial/filter-to` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- reth_development: **change_needed**, `verified-initial/filter-both` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- reth_release: **matches**, `verified-a/filter-all` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- reth_release: **matches**, `verified-initial/filter-all` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- reth_release: **matches**, `verified-initial/filter-from` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- reth_release: **matches**, `verified-initial/filter-to` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- reth_release: **change_needed**, `verified-initial/filter-both` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
