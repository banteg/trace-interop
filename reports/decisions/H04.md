# H04 — Empty address lists

**Disposition:** Spec decision. Proposal, awaiting client review.

## Proposed behavior

Treat an omitted list or [] as no restriction. If null is accepted, give it the same meaning; document whether null is accepted at all. A supplied nonempty list restricts that side.

## Rationale

Programmatically adding an empty optional filter should not erase all results.

## Open questions

Agree whether null is legal; distinguish that schema choice from empty-array matching and special-action membership.

## Evidence

- [filter-all](../../evidence/2026-09-15/cases/filter-all.json)
- [filter-empty](../../evidence/2026-09-15/cases/filter-empty.json)
- [semantic-01--filter-both-null](../../evidence/2026-09-15/cases/semantic-01--filter-both-null.json)
- [semantic-01--filter-from-empty-to-set](../../evidence/2026-09-15/cases/semantic-01--filter-from-empty-to-set.json)
- [semantic-01--filter-to-empty-from-set](../../evidence/2026-09-15/cases/semantic-01--filter-to-empty-from-set.json)

## Observations

- go-ethereum_trace: **matches**, `geth-final-a/filter-from-empty-to-set` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- go-ethereum_trace: **matches**, `geth-final-a/filter-to-empty-from-set` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- go-ethereum_trace: **matches**, `geth-final-initial/filter-empty` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- besu_development: **change_needed**, `verified-a/filter-from-empty-to-set` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- besu_development: **matches**, `verified-a/filter-to-empty-from-set` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- besu_development: **matches**, `verified-initial/filter-empty` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- besu_release: **change_needed**, `verified-a-besu-retry/filter-from-empty-to-set` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- besu_release: **matches**, `verified-a-besu-retry/filter-to-empty-from-set` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- besu_release: **matches**, `verified-initial/filter-empty` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- erigon_development: **matches**, `verified-a/filter-from-empty-to-set` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- erigon_development: **matches**, `verified-a/filter-to-empty-from-set` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- erigon_development: **matches**, `verified-initial/filter-empty` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- erigon_release: **matches**, `verified-a/filter-from-empty-to-set` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- erigon_release: **matches**, `verified-a/filter-to-empty-from-set` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- erigon_release: **matches**, `verified-initial/filter-empty` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- nethermind_development: **change_needed**, `verified-a/filter-from-empty-to-set` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- nethermind_development: **change_needed**, `verified-a/filter-to-empty-from-set` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- nethermind_development: **change_needed**, `verified-initial/filter-empty` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- nethermind_release: **change_needed**, `verified-a/filter-from-empty-to-set` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- nethermind_release: **change_needed**, `verified-a/filter-to-empty-from-set` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- nethermind_release: **change_needed**, `verified-initial/filter-empty` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- reth_development: **matches**, `verified-a/filter-from-empty-to-set` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- reth_development: **matches**, `verified-a/filter-to-empty-from-set` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- reth_development: **matches**, `verified-initial/filter-empty` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- reth_release: **matches**, `verified-a/filter-from-empty-to-set` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- reth_release: **matches**, `verified-a/filter-to-empty-from-set` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- reth_release: **matches**, `verified-initial/filter-empty` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
