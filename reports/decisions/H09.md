# H09 — Failed frame results and error labels

**Disposition:** Spec decision. Proposal, awaiting client review.

## Proposed behavior

Always mark failure explicitly, preserve REVERT return bytes and measured gasUsed when available, and define a small stable set of failure kinds. Human-readable explanations or decoded revertReason may be optional. Use null for an inapplicable result; never manufacture a successful result for an exceptional halt.

## Rationale

Consumers need the raw revert data and failure identity without parsing implementation-specific English or mistaking a failure for success.

## Open questions

Agree exact field placement and stable failure kinds. Separate REVERT, exceptional halt and SELFDESTRUCT/no-result cases in fixtures; do not require identical free-text descriptions.

## Evidence

- [block-tree](../../evidence/2026-09-15/cases/block-tree.json)
- [call-many](../../evidence/2026-09-15/cases/call-many.json)
- [call-many-priced](../../evidence/2026-09-15/cases/call-many-priced.json)
- [call-many-transfers](../../evidence/2026-09-15/cases/call-many-transfers.json)
- [call-tree-trace](../../evidence/2026-09-15/cases/call-tree-trace.json)
- [call-tree-trace-priced](../../evidence/2026-09-15/cases/call-tree-trace-priced.json)
- [filter-all](../../evidence/2026-09-15/cases/filter-all.json)
- [filter-from](../../evidence/2026-09-15/cases/filter-from.json)
- [filter-to](../../evidence/2026-09-15/cases/filter-to.json)
- [filter-union](../../evidence/2026-09-15/cases/filter-union.json)
- [replay-revert-trace](../../evidence/2026-09-15/cases/replay-revert-trace.json)
- [replay-tree-trace](../../evidence/2026-09-15/cases/replay-tree-trace.json)
- [transaction-revert](../../evidence/2026-09-15/cases/transaction-revert.json)
- [transaction-tree](../../evidence/2026-09-15/cases/transaction-tree.json)

## Observations

- besu_development: **change_needed**, `initial/transaction-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_development: **change_needed**, `initial/transaction-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_development: **change_needed**, `initial/block-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_development: **change_needed**, `initial/call-tree-trace-priced` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_development: **change_needed**, `verified-a/transaction-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_development: **change_needed**, `verified-a/call-siblings-ok-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_development: **change_needed**, `verified-a/call-siblings-revert-ok` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_development: **change_needed**, `verified-a/auth-replace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_development: **change_needed**, `verified-a/auth-set-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_development: **change_needed**, `verified-a/block-2` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_development: **change_needed**, `verified-forks/block-36` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_development: **change_needed**, `verified-forks/block-48` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_development: **change_needed**, `verified-forks/block-51` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_development: **change_needed**, `verified-forks/block-60` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_development: **change_needed**, `verified-forks/mcopy-trace-55` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_development: **change_needed**, `verified-initial/transaction-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_development: **change_needed**, `verified-initial/transaction-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_development: **change_needed**, `verified-initial/block-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_development: **change_needed**, `verified-initial/call-tree-trace-priced` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_development: **change_needed**, `verified-repeat/call-siblings-revert-ok` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_development: **change_needed**, `verified-repeat/auth-set-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_release: **change_needed**, `initial/transaction-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_release: **change_needed**, `initial/transaction-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_release: **change_needed**, `initial/block-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_release: **change_needed**, `initial/call-tree-trace-priced` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_release: **change_needed**, `verified-fork-followup/beacon-call-55` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_release: **change_needed**, `verified-forks/block-36` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_release: **change_needed**, `verified-forks/block-48` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_release: **change_needed**, `verified-forks/block-51` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_release: **change_needed**, `verified-forks/block-60` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_release: **change_needed**, `verified-forks/mcopy-trace-55` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_release: **change_needed**, `verified-initial/transaction-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_release: **change_needed**, `verified-initial/transaction-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_release: **change_needed**, `verified-initial/block-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_release: **change_needed**, `verified-initial/call-tree-trace-priced` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_release: **change_needed**, `verified-repeat/call-siblings-revert-ok` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_release: **change_needed**, `verified-repeat/auth-set-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_development: **matches**, `initial/transaction-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_development: **matches**, `initial/replay-tree-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_development: **matches**, `initial/transaction-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_development: **matches**, `initial/replay-revert-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_development: **matches**, `initial/block-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_development: **matches**, `initial/call-tree-trace-priced` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_development: **matches**, `verified-a/transaction-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_development: **matches**, `verified-a/call-siblings-ok-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_development: **matches**, `verified-a/call-siblings-revert-ok` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_development: **matches**, `verified-a/auth-replace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_development: **matches**, `verified-a/auth-set-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_development: **matches**, `verified-a/block-2` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_development: **matches**, `verified-fork-followup/beacon-call-55` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_development: **matches**, `verified-forks/block-36` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_development: **matches**, `verified-forks/block-48` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_development: **matches**, `verified-forks/block-51` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_development: **matches**, `verified-forks/block-60` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_development: **matches**, `verified-forks/mcopy-trace-55` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_development: **matches**, `verified-initial/transaction-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_development: **matches**, `verified-initial/replay-tree-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_development: **matches**, `verified-initial/transaction-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_development: **matches**, `verified-initial/replay-revert-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_development: **matches**, `verified-initial/block-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_development: **matches**, `verified-initial/call-tree-trace-priced` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_development: **matches**, `verified-repeat/call-siblings-revert-ok` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_development: **matches**, `verified-repeat/auth-set-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_release: **matches**, `initial/transaction-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_release: **matches**, `initial/replay-tree-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_release: **matches**, `initial/transaction-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_release: **matches**, `initial/replay-revert-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_release: **matches**, `initial/block-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_release: **matches**, `initial/call-tree-trace-priced` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_release: **matches**, `initial/call-tree-stateDiff-priced` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_release: **matches**, `initial/call-tree-vmTrace-priced` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_release: **matches**, `verified-a/transaction-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_release: **matches**, `verified-a/call-siblings-ok-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_release: **matches**, `verified-a/call-siblings-revert-ok` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_release: **matches**, `verified-a/auth-replace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_release: **matches**, `verified-a/auth-set-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_release: **matches**, `verified-a/block-2` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_release: **matches**, `verified-forks/block-36` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_release: **matches**, `verified-forks/block-48` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_release: **matches**, `verified-forks/block-51` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_release: **matches**, `verified-forks/block-60` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_release: **matches**, `verified-forks/mcopy-trace-55` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_release: **matches**, `verified-initial/transaction-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_release: **matches**, `verified-initial/replay-tree-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_release: **matches**, `verified-initial/transaction-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_release: **matches**, `verified-initial/replay-revert-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_release: **matches**, `verified-initial/block-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_release: **matches**, `verified-initial/call-tree-trace-priced` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_release: **matches**, `verified-initial/call-tree-stateDiff-priced` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_release: **matches**, `verified-initial/call-tree-vmTrace-priced` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_release: **matches**, `verified-repeat/call-siblings-revert-ok` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_release: **matches**, `verified-repeat/auth-set-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_development: **change_needed**, `initial/transaction-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_development: **change_needed**, `initial/replay-tree-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_development: **change_needed**, `initial/transaction-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_development: **change_needed**, `initial/replay-revert-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_development: **change_needed**, `initial/block-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_development: **change_needed**, `initial/call-tree-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_development: **change_needed**, `initial/call-tree-trace-priced` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_development: **change_needed**, `verified-a/transaction-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_development: **change_needed**, `verified-a/call-siblings-ok-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_development: **change_needed**, `verified-a/call-siblings-revert-ok` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_development: **change_needed**, `verified-a/auth-replace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_development: **change_needed**, `verified-a/auth-set-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_development: **change_needed**, `verified-a/block-2` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_development: **change_needed**, `verified-fork-followup/beacon-call-55` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_development: **change_needed**, `verified-forks/block-36` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_development: **change_needed**, `verified-forks/block-48` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_development: **change_needed**, `verified-forks/block-51` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_development: **change_needed**, `verified-forks/block-60` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_development: **change_needed**, `verified-forks/mcopy-trace-55` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_development: **change_needed**, `verified-initial/transaction-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_development: **change_needed**, `verified-initial/replay-tree-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_development: **change_needed**, `verified-initial/transaction-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_development: **change_needed**, `verified-initial/replay-revert-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_development: **change_needed**, `verified-initial/block-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_development: **change_needed**, `verified-initial/call-tree-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_development: **change_needed**, `verified-initial/call-tree-trace-priced` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_development: **change_needed**, `verified-repeat/call-siblings-revert-ok` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_development: **change_needed**, `verified-repeat/auth-set-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_release: **change_needed**, `initial/transaction-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_release: **change_needed**, `initial/replay-tree-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_release: **change_needed**, `initial/transaction-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_release: **change_needed**, `initial/replay-revert-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_release: **change_needed**, `initial/block-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_release: **change_needed**, `initial/call-tree-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_release: **change_needed**, `initial/call-tree-trace-priced` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_release: **change_needed**, `verified-a/transaction-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_release: **change_needed**, `verified-a/call-siblings-ok-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_release: **change_needed**, `verified-a/call-siblings-revert-ok` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_release: **change_needed**, `verified-a/auth-replace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_release: **change_needed**, `verified-a/auth-set-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_release: **change_needed**, `verified-a/block-2` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_release: **change_needed**, `verified-fork-followup/beacon-call-55` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_release: **change_needed**, `verified-forks/block-36` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_release: **change_needed**, `verified-forks/block-48` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_release: **change_needed**, `verified-forks/block-51` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_release: **change_needed**, `verified-forks/block-60` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_release: **change_needed**, `verified-forks/mcopy-trace-55` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_release: **change_needed**, `verified-initial/transaction-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_release: **change_needed**, `verified-initial/replay-tree-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_release: **change_needed**, `verified-initial/transaction-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_release: **change_needed**, `verified-initial/replay-revert-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_release: **change_needed**, `verified-initial/block-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_release: **change_needed**, `verified-initial/call-tree-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_release: **change_needed**, `verified-initial/call-tree-trace-priced` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_release: **change_needed**, `verified-repeat/call-siblings-revert-ok` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_release: **change_needed**, `verified-repeat/auth-set-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_development: **matches**, `initial/transaction-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_development: **matches**, `initial/replay-tree-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_development: **matches**, `initial/transaction-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_development: **matches**, `initial/replay-revert-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_development: **matches**, `initial/block-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_development: **matches**, `initial/call-tree-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_development: **matches**, `initial/call-tree-trace-priced` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_development: **matches**, `verified-a/transaction-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_development: **matches**, `verified-a/call-siblings-ok-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_development: **matches**, `verified-a/call-siblings-revert-ok` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_development: **matches**, `verified-a/auth-replace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_development: **matches**, `verified-a/auth-set-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_development: **matches**, `verified-a/block-2` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_development: **matches**, `verified-fork-followup/beacon-call-55` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_development: **matches**, `verified-forks/block-36` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_development: **matches**, `verified-forks/block-48` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_development: **matches**, `verified-forks/block-51` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_development: **matches**, `verified-forks/block-60` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_development: **matches**, `verified-forks/mcopy-trace-55` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_development: **matches**, `verified-initial/transaction-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_development: **matches**, `verified-initial/replay-tree-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_development: **matches**, `verified-initial/transaction-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_development: **matches**, `verified-initial/replay-revert-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_development: **matches**, `verified-initial/block-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_development: **matches**, `verified-initial/call-tree-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_development: **matches**, `verified-initial/call-tree-trace-priced` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_development: **matches**, `verified-repeat/call-siblings-revert-ok` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_development: **matches**, `verified-repeat/auth-set-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_release: **matches**, `initial/transaction-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_release: **matches**, `initial/replay-tree-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_release: **matches**, `initial/transaction-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_release: **matches**, `initial/replay-revert-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_release: **matches**, `initial/block-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_release: **matches**, `initial/call-tree-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_release: **matches**, `initial/call-tree-trace-priced` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_release: **matches**, `verified-a/transaction-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_release: **matches**, `verified-a/call-siblings-ok-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_release: **matches**, `verified-a/call-siblings-revert-ok` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_release: **matches**, `verified-a/auth-replace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_release: **matches**, `verified-a/auth-set-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_release: **matches**, `verified-a/block-2` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_release: **matches**, `verified-fork-followup/beacon-call-55` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_release: **matches**, `verified-forks/block-36` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_release: **matches**, `verified-forks/block-48` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_release: **matches**, `verified-forks/block-51` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_release: **matches**, `verified-forks/block-60` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_release: **matches**, `verified-forks/mcopy-trace-55` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_release: **matches**, `verified-initial/transaction-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_release: **matches**, `verified-initial/replay-tree-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_release: **matches**, `verified-initial/transaction-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_release: **matches**, `verified-initial/replay-revert-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_release: **matches**, `verified-initial/block-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_release: **matches**, `verified-initial/call-tree-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_release: **matches**, `verified-initial/call-tree-trace-priced` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_release: **matches**, `verified-repeat/call-siblings-revert-ok` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_release: **matches**, `verified-repeat/auth-set-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
