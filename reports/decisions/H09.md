# H09 — Failed frame results and error labels

**Disposition:** Spec decision. Proposal, awaiting client review.

## Proposed behavior

Always mark failure explicitly, preserve REVERT return bytes and measured gasUsed when available, and define a small stable set of failure kinds. Human-readable explanations or decoded revertReason may be optional. Use null for an inapplicable result; never manufacture a successful result for an exceptional halt.

## Rationale

Consumers need the raw revert data and failure identity without parsing implementation-specific English or mistaking a failure for success.

## September 15 observation

Erigon/Reth retain REVERT output and gasUsed in result; Besu/Nethermind omit that result in the replayed tree. Static-write failure labels differ across all four. Besu also supplies revertReason on a top-level revert. Omitted result versus result:null occurs on exceptional frames.

## Unresolved review

Agree exact field placement and stable failure kinds. Separate REVERT, exceptional halt and SELFDESTRUCT/no-result cases in fixtures; do not require identical free-text descriptions.

## Historical evidence

- [block-tree](../../evidence/2026-09-15/cases/block-tree.json)
- [call-many](../../evidence/2026-09-15/cases/call-many.json)
- [call-many-priced](../../evidence/2026-09-15/cases/call-many-priced.json)
- [call-many-transfers](../../evidence/2026-09-15/cases/call-many-transfers.json)
- [call-tree-trace](../../evidence/2026-09-15/cases/call-tree-trace.json)
- [call-tree-trace-priced](../../evidence/2026-09-15/cases/call-tree-trace-priced.json)
- [filter-all](../../evidence/2026-09-15/cases/filter-all.json)
- [filter-from](../../evidence/2026-09-15/cases/filter-from.json)
- [filter-to](../../evidence/2026-09-15/cases/filter-to.json)
- [filter-union](../../evidence/2026-09-15/cases/filter-union.json)
- [replay-revert-trace](../../evidence/2026-09-15/cases/replay-revert-trace.json)
- [replay-tree-trace](../../evidence/2026-09-15/cases/replay-tree-trace.json)
- [transaction-revert](../../evidence/2026-09-15/cases/transaction-revert.json)
- [transaction-tree](../../evidence/2026-09-15/cases/transaction-tree.json)

## Fresh assertions

- besu_development: **change_needed**, `initial/transaction-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_development: **change_needed**, `initial/transaction-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_development: **change_needed**, `initial/block-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_development: **change_needed**, `initial/call-tree-trace-priced` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_release: **change_needed**, `initial/transaction-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_release: **change_needed**, `initial/transaction-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_release: **change_needed**, `initial/block-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- besu_release: **change_needed**, `initial/call-tree-trace-priced` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_development: **matches**, `initial/transaction-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_development: **matches**, `initial/replay-tree-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_development: **matches**, `initial/transaction-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_development: **matches**, `initial/replay-revert-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_development: **matches**, `initial/block-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_development: **matches**, `initial/call-tree-trace-priced` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_release: **matches**, `initial/transaction-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_release: **matches**, `initial/replay-tree-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_release: **matches**, `initial/transaction-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_release: **matches**, `initial/replay-revert-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_release: **matches**, `initial/block-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_release: **matches**, `initial/call-tree-trace-priced` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_release: **matches**, `initial/call-tree-stateDiff-priced` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- erigon_release: **matches**, `initial/call-tree-vmTrace-priced` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_development: **change_needed**, `initial/transaction-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_development: **change_needed**, `initial/replay-tree-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_development: **change_needed**, `initial/transaction-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_development: **change_needed**, `initial/replay-revert-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_development: **change_needed**, `initial/block-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_development: **change_needed**, `initial/call-tree-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_development: **change_needed**, `initial/call-tree-trace-priced` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_release: **change_needed**, `initial/transaction-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_release: **change_needed**, `initial/replay-tree-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_release: **change_needed**, `initial/transaction-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_release: **change_needed**, `initial/replay-revert-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_release: **change_needed**, `initial/block-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_release: **change_needed**, `initial/call-tree-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- nethermind_release: **change_needed**, `initial/call-tree-trace-priced` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_development: **matches**, `initial/transaction-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_development: **matches**, `initial/replay-tree-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_development: **matches**, `initial/transaction-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_development: **matches**, `initial/replay-revert-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_development: **matches**, `initial/block-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_development: **matches**, `initial/call-tree-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_development: **matches**, `initial/call-tree-trace-priced` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_release: **matches**, `initial/transaction-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_release: **matches**, `initial/replay-tree-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_release: **matches**, `initial/transaction-revert` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_release: **matches**, `initial/replay-revert-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_release: **matches**, `initial/block-tree` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_release: **matches**, `initial/call-tree-trace` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
- reth_release: **matches**, `initial/call-tree-trace-priced` — Failed frames have an explicit result; REVERT preserves return bytes and measured gas.
