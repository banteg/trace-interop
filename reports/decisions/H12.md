# H12 — Raw-transaction block argument

**Disposition:** Spec / extension decision. Proposal, awaiting client review.

## Proposed behavior

Standardize the existing two-argument baseline with an explicit default of latest state. Treat an optional third block selector as a separately negotiated extension until clients adopt it; reject unsupported extra arguments consistently.

## Rationale

The baseline should be usable on all four today without silently changing which state is traced.

## September 15 observation

Reth accepts a third block argument; Erigon/Nethermind/Besu reject that three-argument request with -32602. A nonce-correct two-argument request executes on all four.

## Unresolved review

Decide whether to add the block selector later. Keep its acceptance test separate from raw transaction execution fidelity.

## Historical evidence

- [raw-valid](../../evidence/2026-09-15/cases/raw-valid.json)

## Fresh assertions

- besu_development: **matches**, `initial/raw-valid` — The two-argument baseline rejects an extra block selector (extension policy remains open).
- besu_release: **matches**, `initial/raw-valid` — The two-argument baseline rejects an extra block selector (extension policy remains open).
- erigon_development: **matches**, `initial/raw-valid` — The two-argument baseline rejects an extra block selector (extension policy remains open).
- erigon_release: **matches**, `initial/raw-valid` — The two-argument baseline rejects an extra block selector (extension policy remains open).
- nethermind_development: **matches**, `initial/raw-valid` — The two-argument baseline rejects an extra block selector (extension policy remains open).
- nethermind_release: **matches**, `initial/raw-valid` — The two-argument baseline rejects an extra block selector (extension policy remains open).
- reth_development: **change_needed**, `initial/raw-valid` — The two-argument baseline rejects an extra block selector (extension policy remains open).
- reth_release: **change_needed**, `initial/raw-valid` — The two-argument baseline rejects an extra block selector (extension policy remains open).
