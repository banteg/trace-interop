# H13 — Signed transaction nonce validation

**Disposition:** Spec decision / likely client fix. Proposal, awaiting client review.

## Proposed behavior

For trace_rawTransaction, honor the signed transaction’s nonce and validity rules at the selected state. Reject nonce mismatch with a stable transaction-validation error. Use unsigned trace_call for hypothetical execution that deliberately relaxes admission rules.

## Rationale

A signed transaction is a concrete transaction, so tracing should not silently rewrite its nonce or return a partial-looking success.

## Open questions

Agree admission semantics for signed raw transactions; fix malformed responses independently of that policy choice.

## Evidence

- [raw-valid-current-nonce](../../evidence/2026-09-15/cases/raw-valid-current-nonce.json)
- [raw-valid-default-block](../../evidence/2026-09-15/cases/raw-valid-default-block.json)
- [repeat-01--raw-nonce-high](../../evidence/2026-09-15/cases/repeat-01--raw-nonce-high.json)
- [repeat-01--raw-wrong-chain](../../evidence/2026-09-15/cases/repeat-01--raw-wrong-chain.json)
- [repeat-01--raw-insufficient-funds](../../evidence/2026-09-15/cases/repeat-01--raw-insufficient-funds.json)
- [repeat-01--raw-low-gas](../../evidence/2026-09-15/cases/repeat-01--raw-low-gas.json)
- [repeat-01--raw-below-basefee](../../evidence/2026-09-15/cases/repeat-01--raw-below-basefee.json)

## Observations

- go-ethereum_trace: **matches**, `geth-final-a/raw-nonce-high` — A signed nonce mismatch is rejected rather than replaced.
- go-ethereum_trace: **matches**, `geth-final-repeat/raw-nonce-high` — A signed nonce mismatch is rejected rather than replaced.
- besu_development: **change_needed**, `verified-a/raw-nonce-high` — A signed nonce mismatch is rejected rather than replaced.
- besu_development: **change_needed**, `verified-repeat/raw-nonce-high` — A signed nonce mismatch is rejected rather than replaced.
- besu_release: **change_needed**, `verified-a-besu-retry/raw-nonce-high` — A signed nonce mismatch is rejected rather than replaced.
- besu_release: **change_needed**, `verified-repeat/raw-nonce-high` — A signed nonce mismatch is rejected rather than replaced.
- erigon_development: **change_needed**, `verified-a/raw-nonce-high` — A signed nonce mismatch is rejected rather than replaced.
- erigon_development: **change_needed**, `verified-repeat/raw-nonce-high` — A signed nonce mismatch is rejected rather than replaced.
- erigon_release: **change_needed**, `verified-a/raw-nonce-high` — A signed nonce mismatch is rejected rather than replaced.
- erigon_release: **change_needed**, `verified-repeat/raw-nonce-high` — A signed nonce mismatch is rejected rather than replaced.
- nethermind_development: **change_needed**, `verified-a/raw-nonce-high` — A signed nonce mismatch is rejected rather than replaced.
- nethermind_development: **change_needed**, `verified-repeat/raw-nonce-high` — A signed nonce mismatch is rejected rather than replaced.
- nethermind_release: **change_needed**, `verified-a/raw-nonce-high` — A signed nonce mismatch is rejected rather than replaced.
- nethermind_release: **change_needed**, `verified-repeat/raw-nonce-high` — A signed nonce mismatch is rejected rather than replaced.
- reth_development: **matches**, `verified-a/raw-nonce-high` — A signed nonce mismatch is rejected rather than replaced.
- reth_development: **matches**, `verified-repeat/raw-nonce-high` — A signed nonce mismatch is rejected rather than replaced.
- reth_release: **matches**, `verified-a/raw-nonce-high` — A signed nonce mismatch is rejected rather than replaced.
- reth_release: **matches**, `verified-repeat/raw-nonce-high` — A signed nonce mismatch is rejected rather than replaced.
