# Invalid-parameter error codes

**Question:** Which error codes do malformed parameters and rejected unsigned calls return?

H14 · Spec decision · [All decisions](../../decisions/README.md)

**Status: ⚪ Under review** · [Status definitions](../../decisions/README.md#status-key)

No policy conclusion has been recorded. Implementation observations below do not establish client-team agreement.

[Client positions](../../decisions/README.md#client-positions): none recorded.

## Recommendation

An explicit null for an optional parameter or an optional member of a parameter object is the same as omitting it: the default applies, and null keeps its own meaning only where the schema gives it one (a null to creates a contract). Use -32602 for malformed encodings, wrong types, unsupported argument shapes, disagreeing data and input, a priority fee above the fee cap and a non-null override parameter a client does not implement. Report unsigned-call validation rejections with the eth_simulateV1 codes: -38012 base fee too low, -38013 intrinsic gas, -38014 insufficient funds, -38025 init-code size and -38026 client limit; a validation failure without a listed code is -32602. Report signed-transaction rejections with the eth_sendRawTransaction error groups (H13). List each method’s codes in its errors. Every call-object field the schema defines must take effect or cause a rejection; only fields outside the schema are ignored, and filters reject unknown fields. Keep messages implementation-specific; omit internal stack traces.

Callers should distinguish a malformed request from a valid request that cannot execute, without text matching.

## Background

Parity's [RPC error helpers](https://github.com/openethereum/parity-ethereum/blob/55c90d4016505317034e3e98f699af07f5404b63/rpc/src/v1/helpers/errors.rs#L145-L178) distinguished invalid parameters from execution errors. The [raw trace handler](https://github.com/openethereum/parity-ethereum/blob/55c90d4016505317034e3e98f699af07f5404b63/rpc/src/v1/impls/traces.rs#L148-L170) mapped invalid RLP through InvalidParams, while its [transaction-error helper](https://github.com/openethereum/parity-ethereum/blob/55c90d4016505317034e3e98f699af07f5404b63/rpc/src/v1/helpers/errors.rs#L466-L481) used a separate server code for transaction errors. [EIP-1474's error table](https://eips.ethereum.org/EIPS/eip-1474#error-codes) later named `-32602` Invalid params and `-32003` Transaction rejected; aligning categories is better grounded than matching historical message text or Parity's private numeric transaction code.

Parity's [trace filter](https://github.com/openethereum/parity-ethereum/blob/55c90d4016505317034e3e98f699af07f5404b63/rpc/src/v1/types/trace_filter.rs#L27-L46) and [call request](https://github.com/openethereum/parity-ethereum/blob/55c90d4016505317034e3e98f699af07f5404b63/rpc/src/v1/types/call_request.rs#L20-L42) rejected unknown fields. The draft deliberately accepts extra call-object fields for interoperability with shared transaction objects, while keeping filters closed. Fields the schema defines must take effect or be rejected, and conflicting fee fields are rejected. Ignoring extras cannot establish extension support and can hide typos; clients must declare supported extensions and callers must not infer support from success. H02 separately retains integer output paths and hex request indices.

### Revised recommendation (2026-09-24 source review)

The previous recommendation named no code for unsigned fee, funding or intrinsic-gas rejections, and the draft said unknown call-object fields MUST be ignored while treating standard fields as supported "when supported by the selected fork". Together these let a client that lacks a defined field run a different transaction and return a success-shaped trace. Erigon’s `TraceCallParam` declares only `data` among the calldata fields ([trace_adhoc.go](https://github.com/erigontech/erigon/blob/3b4861d10387ca3e19f7b16d1b8c0ec1fcd616cd/rpc/jsonrpc/trace_adhoc.go#L66-L79)), so Go’s decoder drops `input`, `nonce`, `chainId`, `type`, `blobVersionedHashes` and `authorizationList`: a request using the canonical `input` field executes with empty calldata. Nethermind maps `data` into `input` so the last one wins, while Besu and Reth reject a mismatch; requiring agreement is stricter than Geth’s eth_call, which silently prefers `input`, and is a deliberate choice. The eth_simulateV1 codes are already emitted by Geth ([errors.go](https://github.com/banteg/go-ethereum/blob/fa8ecb9242dda61858c44cf43c70d00548fbd7cd/internal/ethapi/errors.go#L103-L160)), Erigon, Nethermind and Reth from their eth_simulateV1 paths; Besu has no -380xx codes. Geth maps tip above cap to -32602 there, which the revision adopts. No corpus request sends `input` or any post-Berlin call field yet, so the Erigon defect is not captured. See [simulation §3, §12 and §13](../../docs/source-review/simulation.md).

### Settled details (2026-09-24, Geth implementation)

Implementing the codes in the Geth draft showed that three listed `eth_simulateV1` codes cannot occur for unsigned calls: a supplied nonce is neither validated nor used (H15), so -38010 and -38011 do not apply; a single call has no block of transactions to exceed (-38015); and unsigned calls skip the EIP-3607 sender-code check exactly as `eth_call` and `eth_simulateV1` do in Geth, so -38024 does not apply. Failures without a specific code, such as a blob fee cap below the blob base fee, a transaction type not active at the fork or an empty authorization list, use -32602 rather than an internal error. The [pinned draft](https://github.com/banteg/execution-apis/commit/53ffb857) states these codes.

### Explicit null is omission (2026-09-26)

A Nethermind review of the `trace_filter` mode field asked whether `"mode": null` should be rejected, since null usually means the default for an optional field. Two throwaway captures on all nine builds answered it. Across the `eth_*` methods, every client, including Geth's own code, treats an explicit null as omitted: null `value`, `data`, `gas`, `gasPrice` and `nonce` in `eth_call`, null `value` in `eth_estimateGas`, null `fromBlock` and `address` in `eth_getLogs`, and null `blockOverrides`, `stateOverrides` and call `value` in `eth_simulateV1` all succeed, although the execution-apis schemas allow none of them. The exceptions are type quirks: Nethermind and Reth reject null for `eth_simulateV1`'s three boolean options, where Besu, Erigon and Geth accept it. In `trace_filter`, null `count` and `toBlock` were accepted by every client build, `after` by Besu, Erigon and Reth, and the address lists by all but Reth; `mode` null was rejected by the three clients that implement the field, each because it decodes an enum, and `fromBlock` split. The draft had followed the schemas' strictness, so the Geth draft alone rejected `trace_call` with a null `value`. The draft now adopts the convention the clients share: an explicit null for an optional member is omitted, and the `trace_*` schemas mark those members nullable. `probes-prague` (`field-null-members`, `field-null-block`, `callmany-null-block`) and `probes-forks` (`filter-null-members`, and null bounds compared with their omitted twins) test it.

Supporting controls (not standalone assertions): [a/filter-from-only-intersection](../cases/a/filter-from-only-intersection.md) · [a/filter-to-only-intersection](../cases/a/filter-to-only-intersection.md) · [probes-prague/field-gas-omitted-eth-call](../cases/probes-prague/field-gas-omitted-eth-call.md).

## What changes for clients

[Test status key](../technical.md#test-status-key) · Build labels show the captured version and source commit.

| Client | Tested builds | Impact | Examples and code |
| --- | --- | --- | --- |
| [Besu](../clients/besu.md) | 26.8.1 · d97cbd61: ⚠️ Differs<br>2 blocked cases: Depends on H15: The zero-address sender is unfunded, so the call runs only if its fees are zero; an error rejects the fee, not the from default. Observed rpc_error -32603 Internal error. 1 policy-open case.<br>26.9-develop · accdae00: ⚠️ Differs<br>2 blocked cases: Depends on H15: The zero-address sender is unfunded, so the call runs only if its fees are zero; an error rejects the fee, not the from default. Observed rpc_error -32603 Internal error. 1 policy-open case. | Wrong address-list types and negative count can return results rather than invalid params.<br>**Proposed:** Validate filter types and bounds and return -32602 for invalid request shapes. Checked requirements: Malformed input returns invalid params (-32602). A chainId that does not match the chain rejects the request. A validation failure without a listed code is invalid params (-32602). | 🧪 Tests: [Filter negative count](../cases/a/filter-negative-count.md) · [Filter wrong address type](../cases/a/filter-wrong-address-type.md)<br>💻 Client code: [Signed transaction replay](https://github.com/besu-eth/besu/blob/f9572aa82a2dadb3dd1b218d3ca97101540faf97/ethereum/api/src/main/java/org/hyperledger/besu/ethereum/api/jsonrpc/internal/methods/TraceRawTransaction.java#L46) · [Call simulation](https://github.com/besu-eth/besu/blob/f9572aa82a2dadb3dd1b218d3ca97101540faf97/ethereum/api/src/main/java/org/hyperledger/besu/ethereum/api/jsonrpc/internal/methods/AbstractTraceCall.java#L40) |
| [Erigon](../clients/erigon.md) | 3.7.0 · bdc78cc4: ⚠️ Differs · [Erigon #24290](https://github.com/erigontech/erigon/pull/24290) (partial fix) · [Erigon #24294](https://github.com/erigontech/erigon/pull/24294) (partial fix)<br>1 blocked case: Depends on H15: The zero-address sender is unfunded, so the call runs only if its fees are zero; an error rejects the fee, not the from default. Observed rpc_error -32000 fee cap less than block base fee: address <nil>, feeCap: 0 baseFee: 765625000. 1 policy-open case.<br>3.8.0-dev · f8cfe5a7: ⚠️ Differs · [Erigon #24290](https://github.com/erigontech/erigon/pull/24290) (partial fix) · [Erigon #24294](https://github.com/erigontech/erigon/pull/24294) (partial fix)<br>1 blocked case: Depends on H15: The zero-address sender is unfunded, so the call runs only if its fees are zero; an error rejects the fee, not the from default. Observed rpc_error -32000 fee cap less than block base fee: address <nil>, feeCap: 0 baseFee: 765625000. 1 policy-open case. | Malformed raw transactions and an unknown option use error codes other than `-32602`. From source, trace_call drops `input` and every post-Berlin call field, so an `input`-only call runs with empty calldata. Open #24290 adds `input`; #24294, merged after this capture, adds `nonce`, `chainId`, `blobVersionedHashes` and `authorizationList`.<br>**Proposed:** Use JSON-RPC invalid params (`-32602`) for malformed input, separately from execution rejection. Decode `input`, `nonce`, `chainId`, `type`, `blobVersionedHashes` and `authorizationList`, or reject them. Checked requirements: Malformed input returns invalid params (-32602). input alone supplies the initcode, which returns word 42. Disagreeing data and input are invalid params (-32602). A valid authorization on an EIP-1559-priced call delegates key 1 to the marker contract, which returns word 42. An omitted from defaults to the zero address, observed by CALLER. An omitted gas runs with the server execution cap, as eth_call does: GAS reports the same value. A matching chainId is accepted and the initcode returns word 42. A chainId that does not match the chain rejects the request. A validation failure without a listed code is invalid params (-32602). | 🧪 Tests: [Call unknown mode](../cases/a/call-unknown-mode.md) · [Filter both unknown mode](../cases/a/filter-both-unknown-mode.md)<br>💻 Client code: [Signed transaction replay](https://github.com/erigontech/erigon/blob/e26d9bd4056586e004488c31b561fb2663d46019/rpc/jsonrpc/trace_adhoc.go#L1695) · [Call simulation](https://github.com/erigontech/erigon/blob/e26d9bd4056586e004488c31b561fb2663d46019/rpc/jsonrpc/trace_adhoc.go#L1096) |
| [Geth draft fork](../clients/geth.md) | 1.17.7-unstable · 0a663f3c: ❔ Policy open<br>1 policy-open case. | Evaluated requirements agree in the checked cases. 1.17.7-unstable · 0a663f3c: Mixing legacy gasPrice with an authorizationList is an open input policy, since no signed transaction type carries both; the response does not isolate the authorization field, which field-authorization-1559 asserts with EIP-1559 fees. Observed: Expected a result; observed rpc_error -32602 gasPrice conflicts with blob or authorization fields Policy remains open; these observations alone do not require a baseline change. | 🧪 Tests: [Field authorization](../cases/probes-prague/field-authorization.md) · [Call null mode](../cases/a/call-null-mode.md)<br>💻 Client code: [Signed transaction replay](https://github.com/banteg/go-ethereum/blob/fa8ecb9242dda61858c44cf43c70d00548fbd7cd/eth/tracers/trace_namespace.go#L98) · [Call simulation](https://github.com/banteg/go-ethereum/blob/fa8ecb9242dda61858c44cf43c70d00548fbd7cd/eth/tracers/trace_namespace.go#L53) |
| [Nethermind](../clients/nethermind.md) | 2.0.0 · bec830cd: ⚠️ Differs<br>1 policy-open case.<br>2.1.0-preview · ee1f57da: ⚠️ Differs<br>1 policy-open case. | Malformed raw input, negative filter count and integer paths expose validation differences across the captured builds.<br>**Proposed:** Return invalid params (`-32602`) for malformed encoding. Checked requirements: Malformed input returns invalid params (-32602). Disagreeing data and input are invalid params (-32602). An access-listed slot of the creation address is warm: PUSH, SLOAD 100, POP and GAS cost 107. A chainId that does not match the chain rejects the request. A validation failure without a listed code is invalid params (-32602). | 🧪 Tests: [Filter both unknown mode](../cases/a/filter-both-unknown-mode.md) · [Filter negative count](../cases/a/filter-negative-count.md)<br>💻 Client code: [Signed transaction replay](https://github.com/NethermindEth/nethermind/blob/641592d2b96fa1e2fa8e8a0b1761582a1728bd51/src/Nethermind/Nethermind.JsonRpc/Modules/Trace/TraceRpcModule.cs#L150) · [Call simulation](https://github.com/NethermindEth/nethermind/blob/641592d2b96fa1e2fa8e8a0b1761582a1728bd51/src/Nethermind/Nethermind.JsonRpc/Modules/Trace/TraceRpcModule.cs#L78) |
| [Reth](../clients/reth.md) | 2.6.0 · 73a3a008: ⚠️ Differs<br>1 policy-open case.<br>2.5.2 · 4630cc58: ⚠️ Differs<br>1 policy-open case. | An integer trace_get path returns null instead of invalid params.<br>**Proposed:** Reject integer path entries with -32602; callers must convert output path integers to hex quantities. Checked requirements: Malformed input returns invalid params (-32602). A validation failure without a listed code is invalid params (-32602). | 🧪 Tests: [Get integer path](../cases/a/get-integer-path.md) · [Field chain id mismatch](../cases/probes-prague/field-chain-id-mismatch.md)<br>💻 Client code: [Signed transaction replay](https://github.com/paradigmxyz/reth/blob/58a51b3ee3f6714ded9207b244a273c8afb592fd/crates/rpc/rpc/src/trace.rs#L121) · [Call simulation](https://github.com/paradigmxyz/reth/blob/58a51b3ee3f6714ded9207b244a273c8afb592fd/crates/rpc/rpc/src/trace.rs#L97) |

## Decision needed

`probes-prague` covers input only, equal and disagreeing data/input, access list (warm-SLOAD gas), authorization list (delegated code), omitted from (CALLER) and gas (eth_call GAS), and a chainId mismatch. The fee and raw-validation checks accept the eth_simulateV1 codes and the error-group codes respectively. Register the null-member cases in `probes-prague` and `probes-forks` under H14, with the omitted-bound twins as references, after their first matrix capture.
