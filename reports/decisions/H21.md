# H21 — vmTrace numeric and optional metadata encoding

**Disposition:** Spec decision. Proposal, awaiting client review.

## Proposed behavior

Encode stack words as minimal hex quantities (zero is 0x0), memory/code as even-length byte strings. Make op/idx optional metadata; require consumers to accept their absence and validate consistency when present. Keep required execution fields independent of these annotations.

## Rationale

Numeric values and byte arrays have different formatting needs; optional convenience labels should not determine whether a trace can be consumed.

## Open questions

Agree schema types and extension policy. A diagnostic comparison may show a separate typed-semantic projection, but must retain the exact wire differences.

## Evidence

- [call-constructor](../../evidence/2026-09-15/cases/call-constructor.json)
- [call-constructor-priced](../../evidence/2026-09-15/cases/call-constructor-priced.json)
- [call-tree-vmTrace](../../evidence/2026-09-15/cases/call-tree-vmTrace.json)
- [call-tree-vmTrace-priced](../../evidence/2026-09-15/cases/call-tree-vmTrace-priced.json)
- [replay-block-tree](../../evidence/2026-09-15/cases/replay-block-tree.json)
- [replay-revert-vmTrace](../../evidence/2026-09-15/cases/replay-revert-vmTrace.json)
- [replay-tree-vmTrace](../../evidence/2026-09-15/cases/replay-tree-vmTrace.json)

## Observations

- besu_development: **matches**, `verified-a/call-return42` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-a/call-siblings-ok-revert` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-a/call-siblings-revert-ok` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-a/call-mixed-create` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-a/call-gas7400` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-a/call-mcopy` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-a/call-destroy` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-a/vm-only-nonempty-output` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-a/prefunded-empty` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-a/raw-valid` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-a/raw-nonce-high` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-a/raw-wrong-chain` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-a/raw-insufficient-funds` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-a/raw-low-gas` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-a/raw-below-basefee` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-a/auth-set` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-a/auth-replace` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-a/auth-clear` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-a/auth-set-revert` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-fork-followup-besu-retry/destroy-trace-55` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-fork-followup-besu-retry/destroy-trace-56` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-forks/destroy-trace-55` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-forks/mcopy-trace-55` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-forks/destroy-trace-56` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-forks/mcopy-trace-56` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-initial/call-tree-vmTrace-priced` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-initial/call-constructor-priced` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-initial/raw-valid-default-block` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-initial/raw-valid-current-nonce` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-initial/call-transfer-vmTrace` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-initial/call-identity` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-repeat/call-return42` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-repeat/call-siblings-revert-ok` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-repeat/call-mixed-create` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-repeat/call-gas7400` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-repeat/call-mcopy` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-repeat/raw-valid` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-repeat/raw-nonce-high` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-repeat/raw-wrong-chain` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-repeat/raw-insufficient-funds` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-repeat/raw-low-gas` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-repeat/raw-below-basefee` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-repeat/auth-clear` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-repeat/auth-set-revert` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-repeat/raw-valid-vmTrace` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-repeat/raw-nonce-high-vmTrace` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-repeat/raw-wrong-chain-vmTrace` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-repeat/raw-insufficient-funds-vmTrace` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-repeat/raw-low-gas-vmTrace` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-repeat/raw-below-basefee-vmTrace` — Stack words use minimal hex quantities at every depth.
- besu_development: **matches**, `verified-repeat/constructor` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-a/call-return42` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-a/call-siblings-ok-revert` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-a/call-siblings-revert-ok` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-a/call-mixed-create` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-a/call-gas7400` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-a/call-mcopy` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-a/call-destroy` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-a/vm-only-nonempty-output` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-a/prefunded-empty` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-a/raw-valid` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-a/raw-nonce-high` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-a/raw-insufficient-funds` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-a/auth-set` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-a/auth-replace` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-a/auth-clear` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-a/auth-set-revert` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-fork-followup/destroy-trace-55` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-fork-followup/destroy-trace-56` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-forks/destroy-trace-55` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-forks/mcopy-trace-55` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-forks/destroy-trace-56` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-forks/mcopy-trace-56` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-initial/replay-tree-vmTrace` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-initial/replay-revert-vmTrace` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-initial/replay-7702-vmTrace` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-initial/call-tree-vmTrace-priced` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-initial/call-constructor-priced` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-initial/raw-valid-default-block` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-initial/raw-valid-current-nonce` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-initial/call-transfer-vmTrace` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-initial/call-identity` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-initial/replay-transfer-vmTrace` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-repeat/call-return42` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-repeat/call-siblings-revert-ok` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-repeat/call-mixed-create` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-repeat/call-gas7400` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-repeat/call-mcopy` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-repeat/raw-valid` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-repeat/raw-nonce-high` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-repeat/raw-insufficient-funds` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-repeat/auth-clear` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-repeat/auth-set-revert` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-repeat/raw-valid-vmTrace` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-repeat/raw-nonce-high-vmTrace` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-repeat/raw-insufficient-funds-vmTrace` — Stack words use minimal hex quantities at every depth.
- erigon_development: **matches**, `verified-repeat/constructor` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-a/call-return42` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-a/call-siblings-ok-revert` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-a/call-siblings-revert-ok` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-a/call-mixed-create` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-a/call-gas7400` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-a/call-mcopy` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-a/call-destroy` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-a/vm-only-nonempty-output` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-a/prefunded-empty` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-a/raw-valid` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-a/raw-nonce-high` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-a/raw-insufficient-funds` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-a/auth-set` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-a/auth-replace` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-a/auth-clear` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-a/auth-set-revert` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-fork-followup/destroy-trace-55` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-fork-followup/destroy-trace-56` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-forks/destroy-trace-55` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-forks/mcopy-trace-55` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-forks/destroy-trace-56` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-forks/mcopy-trace-56` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-initial/replay-tree-vmTrace` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-initial/replay-revert-vmTrace` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-initial/replay-7702-vmTrace` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-initial/call-tree-vmTrace-priced` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-initial/call-constructor-priced` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-initial/raw-valid-default-block` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-initial/raw-valid-current-nonce` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-initial/call-transfer-vmTrace` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-initial/call-identity` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-initial/replay-transfer-vmTrace` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-repeat/call-return42` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-repeat/call-siblings-revert-ok` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-repeat/call-mixed-create` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-repeat/call-gas7400` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-repeat/call-mcopy` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-repeat/raw-valid` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-repeat/raw-nonce-high` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-repeat/raw-insufficient-funds` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-repeat/auth-clear` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-repeat/auth-set-revert` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-repeat/raw-valid-vmTrace` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-repeat/raw-nonce-high-vmTrace` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-repeat/raw-insufficient-funds-vmTrace` — Stack words use minimal hex quantities at every depth.
- erigon_release: **matches**, `verified-repeat/constructor` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **change_needed**, `verified-a/call-return42` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **change_needed**, `verified-a/call-siblings-ok-revert` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **change_needed**, `verified-a/call-siblings-revert-ok` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **change_needed**, `verified-a/call-mixed-create` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **change_needed**, `verified-a/call-gas7400` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **change_needed**, `verified-a/call-mcopy` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **matches**, `verified-a/call-destroy` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **change_needed**, `verified-a/vm-only-nonempty-output` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **matches**, `verified-a/prefunded-empty` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **matches**, `verified-a/raw-valid` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **matches**, `verified-a/raw-nonce-high` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **change_needed**, `verified-a/auth-set` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **change_needed**, `verified-a/auth-replace` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **matches**, `verified-a/auth-clear` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **change_needed**, `verified-a/auth-set-revert` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **matches**, `verified-fork-followup/destroy-trace-55` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **matches**, `verified-fork-followup/destroy-trace-56` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **matches**, `verified-forks/destroy-trace-55` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **change_needed**, `verified-forks/mcopy-trace-55` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **matches**, `verified-forks/destroy-trace-56` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **change_needed**, `verified-forks/mcopy-trace-56` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **change_needed**, `verified-initial/replay-tree-vmTrace` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **change_needed**, `verified-initial/replay-revert-vmTrace` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **matches**, `verified-initial/replay-7702-vmTrace` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **change_needed**, `verified-initial/call-tree-vmTrace` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **change_needed**, `verified-initial/call-constructor` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **change_needed**, `verified-initial/call-tree-vmTrace-priced` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **change_needed**, `verified-initial/call-constructor-priced` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **matches**, `verified-initial/raw-valid-default-block` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **matches**, `verified-initial/raw-valid-current-nonce` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **matches**, `verified-initial/call-transfer-vmTrace` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **matches**, `verified-initial/call-identity` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **matches**, `verified-initial/replay-transfer-vmTrace` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **change_needed**, `verified-repeat/call-return42` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **change_needed**, `verified-repeat/call-siblings-revert-ok` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **change_needed**, `verified-repeat/call-mixed-create` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **change_needed**, `verified-repeat/call-gas7400` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **change_needed**, `verified-repeat/call-mcopy` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **matches**, `verified-repeat/raw-valid` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **matches**, `verified-repeat/raw-nonce-high` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **matches**, `verified-repeat/auth-clear` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **change_needed**, `verified-repeat/auth-set-revert` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **matches**, `verified-repeat/raw-valid-vmTrace` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **matches**, `verified-repeat/raw-nonce-high-vmTrace` — Stack words use minimal hex quantities at every depth.
- nethermind_development: **change_needed**, `verified-repeat/constructor` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **change_needed**, `verified-a/call-return42` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **change_needed**, `verified-a/call-siblings-ok-revert` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **change_needed**, `verified-a/call-siblings-revert-ok` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **change_needed**, `verified-a/call-mixed-create` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **change_needed**, `verified-a/call-gas7400` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **change_needed**, `verified-a/call-mcopy` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **matches**, `verified-a/call-destroy` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **change_needed**, `verified-a/vm-only-nonempty-output` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **matches**, `verified-a/prefunded-empty` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **matches**, `verified-a/raw-valid` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **matches**, `verified-a/raw-nonce-high` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **change_needed**, `verified-a/auth-set` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **change_needed**, `verified-a/auth-replace` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **matches**, `verified-a/auth-clear` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **change_needed**, `verified-a/auth-set-revert` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **matches**, `verified-fork-followup/destroy-trace-55` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **matches**, `verified-fork-followup/destroy-trace-56` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **matches**, `verified-forks/destroy-trace-55` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **change_needed**, `verified-forks/mcopy-trace-55` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **matches**, `verified-forks/destroy-trace-56` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **change_needed**, `verified-forks/mcopy-trace-56` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **change_needed**, `verified-initial/replay-tree-vmTrace` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **change_needed**, `verified-initial/replay-revert-vmTrace` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **matches**, `verified-initial/replay-7702-vmTrace` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **change_needed**, `verified-initial/call-tree-vmTrace` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **change_needed**, `verified-initial/call-constructor` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **change_needed**, `verified-initial/call-tree-vmTrace-priced` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **change_needed**, `verified-initial/call-constructor-priced` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **matches**, `verified-initial/raw-valid-default-block` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **matches**, `verified-initial/raw-valid-current-nonce` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **matches**, `verified-initial/call-transfer-vmTrace` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **matches**, `verified-initial/call-identity` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **matches**, `verified-initial/replay-transfer-vmTrace` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **change_needed**, `verified-repeat/call-return42` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **change_needed**, `verified-repeat/call-siblings-revert-ok` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **change_needed**, `verified-repeat/call-mixed-create` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **change_needed**, `verified-repeat/call-gas7400` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **change_needed**, `verified-repeat/call-mcopy` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **matches**, `verified-repeat/raw-valid` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **matches**, `verified-repeat/raw-nonce-high` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **matches**, `verified-repeat/auth-clear` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **change_needed**, `verified-repeat/auth-set-revert` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **matches**, `verified-repeat/raw-valid-vmTrace` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **matches**, `verified-repeat/raw-nonce-high-vmTrace` — Stack words use minimal hex quantities at every depth.
- nethermind_release: **change_needed**, `verified-repeat/constructor` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-a/call-return42` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-a/call-siblings-ok-revert` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-a/call-siblings-revert-ok` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-a/call-mixed-create` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-a/call-gas7400` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-a/call-mcopy` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-a/call-destroy` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-a/vm-only-nonempty-output` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-a/prefunded-empty` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-a/raw-valid` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-a/auth-set` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-a/auth-replace` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-a/auth-clear` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-a/auth-set-revert` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-fork-followup/destroy-trace-55` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-fork-followup/destroy-trace-56` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-forks/destroy-trace-55` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-forks/mcopy-trace-55` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-forks/destroy-trace-56` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-forks/mcopy-trace-56` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-initial/replay-tree-vmTrace` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-initial/replay-revert-vmTrace` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-initial/replay-7702-vmTrace` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-initial/call-tree-vmTrace` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-initial/call-constructor` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-initial/raw-valid` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-initial/call-tree-vmTrace-priced` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-initial/call-constructor-priced` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-initial/raw-valid-current-nonce` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-initial/call-transfer-vmTrace` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-initial/call-identity` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-initial/replay-transfer-vmTrace` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-repeat/call-return42` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-repeat/call-siblings-revert-ok` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-repeat/call-mixed-create` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-repeat/call-gas7400` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-repeat/call-mcopy` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-repeat/raw-valid` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-repeat/auth-clear` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-repeat/auth-set-revert` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-repeat/raw-valid-vmTrace` — Stack words use minimal hex quantities at every depth.
- reth_development: **matches**, `verified-repeat/constructor` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-a/call-return42` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-a/call-siblings-ok-revert` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-a/call-siblings-revert-ok` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-a/call-mixed-create` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-a/call-gas7400` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-a/call-mcopy` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-a/call-destroy` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-a/vm-only-nonempty-output` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-a/prefunded-empty` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-a/raw-valid` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-a/auth-set` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-a/auth-replace` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-a/auth-clear` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-a/auth-set-revert` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-fork-followup/destroy-trace-55` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-fork-followup/destroy-trace-56` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-forks/destroy-trace-55` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-forks/mcopy-trace-55` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-forks/destroy-trace-56` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-forks/mcopy-trace-56` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-initial/replay-tree-vmTrace` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-initial/replay-revert-vmTrace` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-initial/replay-7702-vmTrace` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-initial/call-tree-vmTrace` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-initial/call-constructor` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-initial/raw-valid` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-initial/call-tree-vmTrace-priced` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-initial/call-constructor-priced` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-initial/raw-valid-current-nonce` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-initial/call-transfer-vmTrace` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-initial/call-identity` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-initial/replay-transfer-vmTrace` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-repeat/call-return42` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-repeat/call-siblings-revert-ok` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-repeat/call-mixed-create` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-repeat/call-gas7400` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-repeat/call-mcopy` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-repeat/raw-valid` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-repeat/auth-clear` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-repeat/auth-set-revert` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-repeat/raw-valid-vmTrace` — Stack words use minimal hex quantities at every depth.
- reth_release: **matches**, `verified-repeat/constructor` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-a-besu-retry/call-return42` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-a-besu-retry/call-siblings-ok-revert` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-a-besu-retry/call-siblings-revert-ok` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-a-besu-retry/call-mixed-create` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-a-besu-retry/call-gas7400` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-a-besu-retry/call-mcopy` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-a-besu-retry/call-destroy` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-a-besu-retry/vm-only-nonempty-output` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-a-besu-retry/prefunded-empty` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-a-besu-retry/raw-valid` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-a-besu-retry/raw-nonce-high` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-a-besu-retry/raw-wrong-chain` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-a-besu-retry/raw-insufficient-funds` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-a-besu-retry/raw-low-gas` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-a-besu-retry/raw-below-basefee` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-a-besu-retry/auth-set` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-a-besu-retry/auth-replace` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-a-besu-retry/auth-clear` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-a-besu-retry/auth-set-revert` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-fork-followup/destroy-trace-55` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-fork-followup/destroy-trace-56` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-forks/destroy-trace-55` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-forks/mcopy-trace-55` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-forks/destroy-trace-56` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-forks/mcopy-trace-56` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-initial/call-tree-vmTrace-priced` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-initial/call-constructor-priced` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-initial/raw-valid-default-block` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-initial/raw-valid-current-nonce` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-initial/call-transfer-vmTrace` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-initial/call-identity` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-repeat/call-return42` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-repeat/call-siblings-revert-ok` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-repeat/call-mixed-create` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-repeat/call-gas7400` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-repeat/call-mcopy` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-repeat/raw-valid` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-repeat/raw-nonce-high` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-repeat/raw-wrong-chain` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-repeat/raw-insufficient-funds` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-repeat/raw-low-gas` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-repeat/raw-below-basefee` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-repeat/auth-clear` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-repeat/auth-set-revert` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-repeat/raw-valid-vmTrace` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-repeat/raw-nonce-high-vmTrace` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-repeat/raw-wrong-chain-vmTrace` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-repeat/raw-insufficient-funds-vmTrace` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-repeat/raw-low-gas-vmTrace` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-repeat/raw-below-basefee-vmTrace` — Stack words use minimal hex quantities at every depth.
- besu_release: **matches**, `verified-repeat/constructor` — Stack words use minimal hex quantities at every depth.
