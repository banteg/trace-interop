# H22 — Precompile return bytes

**Disposition:** Likely client bug. Proposal, awaiting client review.

## Proposed behavior

Report the actual return bytes in both the execution envelope and the corresponding successful call-frame result, including precompiles with no EVM bytecode.

## Rationale

Two views of the same successful call should not disagree about its return value.

## Open questions

A focused Besu reporting fix and regression fixture can proceed without waiting on broad API policy.

## Evidence

- [call-identity](../../evidence/2026-09-15/cases/call-identity.json)

## Observations

- go-ethereum_trace: **matches**, `geth-final-initial/call-identity` — The identity precompile call frame preserves its input as return bytes.
- besu_development: **change_needed**, `verified-initial/call-identity` — The identity precompile call frame preserves its input as return bytes.
- besu_release: **change_needed**, `verified-initial/call-identity` — The identity precompile call frame preserves its input as return bytes.
- erigon_development: **matches**, `verified-initial/call-identity` — The identity precompile call frame preserves its input as return bytes.
- erigon_release: **matches**, `verified-initial/call-identity` — The identity precompile call frame preserves its input as return bytes.
- nethermind_development: **matches**, `verified-initial/call-identity` — The identity precompile call frame preserves its input as return bytes.
- nethermind_release: **matches**, `verified-initial/call-identity` — The identity precompile call frame preserves its input as return bytes.
- reth_development: **matches**, `verified-initial/call-identity` — The identity precompile call frame preserves its input as return bytes.
- reth_release: **matches**, `verified-initial/call-identity` — The identity precompile call frame preserves its input as return bytes.
