# H22 — Precompile return bytes

**Disposition:** Likely client bug. Proposal, awaiting client review.

## Proposed behavior

Report the actual return bytes in both the execution envelope and the corresponding successful call-frame result, including precompiles with no EVM bytecode.

## Rationale

Two views of the same successful call should not disagree about its return value.

## September 15 observation

All four return top-level output 0x11223344 from the identity precompile. Erigon/Nethermind/Reth repeat those bytes in trace[0].result.output; Besu returns 0x there.

## Unresolved review

A focused Besu reporting fix and regression fixture can proceed without waiting on broad API policy.

## Historical evidence

- [call-identity](../../evidence/2026-09-15/cases/call-identity.json)

## Fresh assertions

- besu_development: **change_needed**, `initial/call-identity` — The identity precompile call frame preserves its input as return bytes.
- besu_release: **change_needed**, `initial/call-identity` — The identity precompile call frame preserves its input as return bytes.
- erigon_development: **matches**, `initial/call-identity` — The identity precompile call frame preserves its input as return bytes.
- erigon_release: **matches**, `initial/call-identity` — The identity precompile call frame preserves its input as return bytes.
- nethermind_development: **matches**, `initial/call-identity` — The identity precompile call frame preserves its input as return bytes.
- nethermind_release: **matches**, `initial/call-identity` — The identity precompile call frame preserves its input as return bytes.
- reth_development: **matches**, `initial/call-identity` — The identity precompile call frame preserves its input as return bytes.
- reth_release: **matches**, `initial/call-identity` — The identity precompile call frame preserves its input as return bytes.
