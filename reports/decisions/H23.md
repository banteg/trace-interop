# H23 — Special-action address matching

**Disposition:** Spec decision / client discrepancy. Proposal, awaiting client review.

## Proposed behavior

Define from/to equivalents per action: creator/created address for CREATE, destroyed account/refund beneficiary for SELFDESTRUCT. Include these records when the corresponding address matches.

## Rationale

Address-based history should not silently omit a transfer or creation because its action uses different field names.

## Open questions

Agree the action mapping and add the discriminating filter fixtures.

## Evidence

- [semantic-01--filter-created-to](../../evidence/2026-09-15/cases/semantic-01--filter-created-to.json)
- [semantic-01--filter-suicide-from](../../evidence/2026-09-15/cases/semantic-01--filter-suicide-from.json)
- [semantic-01--filter-suicide-beneficiary](../../evidence/2026-09-15/cases/semantic-01--filter-suicide-beneficiary.json)
- [semantic-01--transaction-tree](../../evidence/2026-09-15/cases/semantic-01--transaction-tree.json)

## Observations

- besu_development: **change_needed**, `verified-a/filter-created-to` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- besu_development: **matches**, `verified-a/filter-creator-from` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- besu_development: **change_needed**, `verified-a/filter-suicide-from` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- besu_development: **change_needed**, `verified-a/filter-suicide-beneficiary` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- erigon_development: **matches**, `verified-a/filter-created-to` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- erigon_development: **matches**, `verified-a/filter-creator-from` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- erigon_development: **matches**, `verified-a/filter-suicide-from` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- erigon_development: **matches**, `verified-a/filter-suicide-beneficiary` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- erigon_release: **matches**, `verified-a/filter-created-to` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- erigon_release: **matches**, `verified-a/filter-creator-from` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- erigon_release: **matches**, `verified-a/filter-suicide-from` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- erigon_release: **matches**, `verified-a/filter-suicide-beneficiary` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- nethermind_development: **matches**, `verified-a/filter-created-to` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- nethermind_development: **matches**, `verified-a/filter-creator-from` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- nethermind_development: **matches**, `verified-a/filter-suicide-from` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- nethermind_development: **matches**, `verified-a/filter-suicide-beneficiary` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- nethermind_release: **matches**, `verified-a/filter-created-to` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- nethermind_release: **matches**, `verified-a/filter-creator-from` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- nethermind_release: **matches**, `verified-a/filter-suicide-from` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- nethermind_release: **matches**, `verified-a/filter-suicide-beneficiary` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- reth_development: **matches**, `verified-a/filter-created-to` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- reth_development: **matches**, `verified-a/filter-creator-from` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- reth_development: **matches**, `verified-a/filter-suicide-from` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- reth_development: **matches**, `verified-a/filter-suicide-beneficiary` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- reth_release: **matches**, `verified-a/filter-created-to` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- reth_release: **matches**, `verified-a/filter-creator-from` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- reth_release: **matches**, `verified-a/filter-suicide-from` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
- reth_release: **matches**, `verified-a/filter-suicide-beneficiary` — Address matching is OR within each list, AND across lists, with action-specific endpoints.
