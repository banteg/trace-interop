# Special-action address matching

H23 · Spec decision / client discrepancy · [All decisions](../../decisions/README.md)

**Status: ⚪ Under review** · [Status definitions](../../decisions/README.md#status-key)

No policy conclusion has been recorded. Implementation observations below do not establish client-team agreement.

[Client positions](../../decisions/README.md#client-positions): none recorded.

## Recommendation

Define from/to equivalents per action: creator and successfully created address for CREATE; the executing (self-destructing) account and beneficiary for SELFDESTRUCT, which after EIP-6780 usually survives; and recipient only for rewards. A failed CREATE has no created-address match: it can match its creator, but not a populated recipient list. A reward matches toAddress by author and has no from side: in intersection mode it is excluded whenever fromAddress is populated, while in union mode a toAddress match suffices. Apply H03 composition, so union can retain a creator match. Matching a reverted action does not imply a committed transfer.

Address-based history should not silently omit a transfer or creation because its action uses different field names.

## Background

Parity's [original address matcher](https://github.com/openethereum/parity-ethereum/blob/55c90d4016505317034e3e98f699af07f5404b63/ethcore/trace/src/types/filter.rs#L82-L110) assigns sender/recipient equivalents per action: CREATE uses the creator and successful result address, SELFDESTRUCT uses the destroyed account and refund address, and PoW reward uses its author as the recipient. This mapping is part of the indexed filter implementation, not a reinterpretation of a generic `to` field. The pinned Besu CREATE and SELFDESTRUCT cases test that the same records visible in `trace_block` can be found by their relevant addresses.

[Parity issue #9822](https://github.com/openethereum/parity-ethereum/issues/9822) reported that `trace_filter` dropped a failed contract creation visible by transaction/block tracing; [the fix](https://github.com/openethereum/parity-ethereum/commit/1ac1224cd3f983d66d650e78428d274350c84a30) addressed that historical edge. a failed CREATE has no successful `result.address`. Parity's matcher still allows a creator-only query to find it but cannot satisfy a nonempty recipient filter. The proposed mapping preserves that distinction instead of assigning a successful destination to a failed creation.

Reward and failed-CREATE matching diverge in source, and no fixture covers them yet. Besu filters rewards through the generic `action.getFrom()` and `action.getTo()` ([TraceFlatTransactionStep.java](https://github.com/besu-eth/besu/blob/07f0a4b2f832257842db71eacf4bca584f87fafe/ethereum/api/src/main/java/org/hyperledger/besu/ethereum/api/jsonrpc/internal/methods/TraceFlatTransactionStep.java#L61-L80)) and Nethermind through `action.From` and `action.To` ([TxTraceFilter.cs](https://github.com/NethermindEth/nethermind/blob/ac02224f25fd4901116ebf2643d522ec4a0e60a1/src/Nethermind/Nethermind.JsonRpc/Modules/Trace/TxTraceFilter.cs#L41-L57)); a reward action carries only `author`, so `toAddress: [miner]` never returns rewards in either client. Erigon emits a reward whenever the coinbase is in `toAddress` ([trace_filtering.go](https://github.com/erigontech/erigon/blob/3b4861d10387ca3e19f7b16d1b8c0ec1fcd616cd/rpc/jsonrpc/trace_filtering.go#L597-L617)), ignoring `mode` and `fromAddress`, so an intersection with a populated sender list still returns it. Nethermind sets a failed CREATE’s `action.To` to the would-be address ([ParityLikeTxTracer.cs](https://github.com/NethermindEth/nethermind/blob/ac02224f25fd4901116ebf2643d522ec4a0e60a1/src/Nethermind/Nethermind.Blockchain/Tracing/ParityStyle/ParityLikeTxTracer.cs#L413-L421)) and matches on it; Reth’s Alloy matcher uses a failed CREATE’s `result.address`, which Reth and Erigon still report (H09). Reth and the Geth draft otherwise match the draft.

### Revised recommendation (2026-09-24 source review)

The previous wording called the SELFDESTRUCT side the "destroyed account", which is inaccurate after EIP-6780, when the account usually survives, and did not spell out reward matching under each mode. The matching itself is unchanged and follows Parity ([filter.rs](https://github.com/openethereum/parity-ethereum/blob/55c90d4016505317034e3e98f699af07f5404b63/ethcore/trace/src/types/filter.rs#L96-L121)). Writing the reward rule down exposes Besu and Nethermind (rewards unreachable through `toAddress`) and Erigon (rewards ignore `mode`). See [filter-selection §2, §5 and §11](../../docs/source-review/filter-selection.md).

Supporting controls (not standalone assertions): [a/transaction-tree](../cases/a/transaction-tree.md).

## What changes for clients

[Test status key](../technical.md#test-status-key) · Build labels show the captured version and source commit.

| Client | Tested builds | Impact | Examples and code |
| --- | --- | --- | --- |
| [Besu](../clients/besu.md) | 26.8.1 · d97cbd61: ⚠️ Differs<br>1 blocked case: Depends on H03: The request names the default mode explicitly, so a server that rejects the mode field fails before matching rewards. Observed rpc_error -32602 Invalid filter params. 1 blocked case: The RPC returned an error, so there is no execution result to inspect.<br>26.9-develop · cf89071f: ⚠️ Differs<br>1 blocked case: Depends on H03: The request names the default mode explicitly, so a server that rejects the mode field fails before matching rewards. Observed rpc_error -32602 Invalid filter params. 1 blocked case: The RPC returned an error, so there is no execution result to inspect. | CREATE and SELFDESTRUCT address queries disagree with the corresponding block trace.<br>**Proposed:** Match creation by creator/created address, SELFDESTRUCT by executing account/beneficiary, and rewards by `author` on the recipient side. Checked requirements: Compare address bytes: OR within each list, AND across lists by default and OR under mode union; missing/null/empty lists are unrestricted. A reward matches toAddress by author, after its block's transactions: block reward, then uncle rewards in ommer order. In union mode a toAddress match by author suffices for a reward; sender roots precede their block's rewards. A created-then-destroyed address matches its creation and its SELFDESTRUCT beneficiary. A SELFDESTRUCT matches fromAddress by its executing account. A SELFDESTRUCT to self also matches toAddress as its own beneficiary. | 🧪 Tests: [Filter created to](../cases/a/filter-created-to.md) · [Filter suicide beneficiary](../cases/a/filter-suicide-beneficiary.md)<br>💻 Client code: [Filter execution pipeline](https://github.com/besu-eth/besu/blob/f9572aa82a2dadb3dd1b218d3ca97101540faf97/ethereum/api/src/main/java/org/hyperledger/besu/ethereum/api/jsonrpc/internal/methods/TraceFilter.java#L135) |
| [Erigon](../clients/erigon.md) | 3.6.1 · 0c4d9c91: ⚠️ Differs<br>3.8.0-dev · 01c118ee: ⚠️ Differs | From source: rewards are returned whenever the coinbase is in `toAddress`, ignoring `mode` and `fromAddress`.<br>**Proposed:** Exclude rewards when `fromAddress` is populated in intersection mode. Checked requirements: A failed top-level CREATE has no created-address match. A failed nested CREATE has no created-address match. A failed CREATE has no recipient side, so an intersection with its would-be address is empty. | 🧪 Tests: [Filter failed create intersection](../cases/mined-probes/filter-failed-create-intersection.md) · [Filter failed create recipient](../cases/mined-probes/filter-failed-create-recipient.md)<br>💻 Client code: [Address filtering](https://github.com/erigontech/erigon/blob/e26d9bd4056586e004488c31b561fb2663d46019/rpc/jsonrpc/trace_filtering.go#L311) |
| [Geth draft fork](../clients/geth.md) | 1.17.7-unstable · 0a663f3c: ✅ Checked cases agree | No change identified in the checked cases. | 🧪 Tests: [Filter created to](../cases/a/filter-created-to.md) · [Filter creator from](../cases/a/filter-creator-from.md)<br>💻 Client code: [Address filtering](https://github.com/banteg/go-ethereum/blob/fa8ecb9242dda61858c44cf43c70d00548fbd7cd/eth/tracers/trace_namespace.go#L187) |
| [Nethermind](../clients/nethermind.md) | 2.0.0 · bec830cd: ⚠️ Differs<br>2.1.0-preview · 54b760cd: ⚠️ Differs | From source: rewards never match `toAddress`, and a failed CREATE matches its would-be address.<br>**Proposed:** Match rewards by `author` on the recipient side, and give a failed CREATE no recipient match. Checked requirements: A reward matches toAddress by author, after its block's transactions: block reward, then uncle rewards in ommer order. In union mode a toAddress match by author suffices for a reward; sender roots precede their block's rewards. A failed top-level CREATE has no created-address match. A failed nested CREATE has no created-address match. A failed CREATE has no recipient side, so an intersection with its would-be address is empty. | 🧪 Tests: [Filter failed create intersection](../cases/mined-probes/filter-failed-create-intersection.md) · [Filter failed create recipient](../cases/mined-probes/filter-failed-create-recipient.md)<br>💻 Client code: [Address matching](https://github.com/NethermindEth/nethermind/blob/641592d2b96fa1e2fa8e8a0b1761582a1728bd51/src/Nethermind/Nethermind.JsonRpc/Modules/Trace/TxTraceFilter.cs#L56) |
| [Reth](../clients/reth.md) | 2.6.0 · 73a3a008: ⚠️ Differs<br>2.5.2 · 58a51b3e: ⚠️ Differs | The linked case differs from the proposed behavior.<br>**Proposed:** A failed top-level CREATE has no created-address match. A failed nested CREATE has no created-address match. A failed CREATE has no recipient side, so an intersection with its would-be address is empty. A SELFDESTRUCT matches fromAddress by its executing account. A SELFDESTRUCT to self also matches toAddress as its own beneficiary. | 🧪 Tests: [Filter failed create intersection](../cases/mined-probes/filter-failed-create-intersection.md) · [Filter failed create recipient](../cases/mined-probes/filter-failed-create-recipient.md)<br>💻 Client code: [Address filtering](https://github.com/paradigmxyz/reth/blob/58a51b3ee3f6714ded9207b244a273c8afb592fd/crates/rpc/rpc/src/trace.rs#L370) |

## Decision needed

`probes-forks` checks reward matching by author, intersection and union with a sender list on the PoW blocks. The mined-probes filters query mined top-level and nested reverted CREATEs by their would-be addresses and creators, and SELFDESTRUCT to self by both sides; the filter oracles ignore a failed CREATE’s result address.
