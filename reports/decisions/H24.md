# H24 — Sibling failure isolation

**Disposition:** Reproduced reporting defect. Proposal, awaiting client review.

## Proposed behavior

Attach error and result data to the actual call frame. A sibling failure must not change another frame's status.

## Rationale

A successful caller can handle a failed subcall and continue; frame results must preserve that distinction.

## Open questions

Prepare a focused Besu regression and fix without waiting for broad schema policy.

## Evidence

- [repeat-01--call-siblings-revert-ok](../../evidence/2026-09-15/cases/repeat-01--call-siblings-revert-ok.json)
- [repeat-01--debug-siblings-revert-ok](../../evidence/2026-09-15/cases/repeat-01--debug-siblings-revert-ok.json)

## Observations

- besu_development: **change_needed**, `verified-a/call-siblings-revert-ok` — The successful second sibling retains its output and has no error.
- besu_development: **change_needed**, `verified-repeat/call-siblings-revert-ok` — The successful second sibling retains its output and has no error.
- besu_release: **change_needed**, `verified-repeat/call-siblings-revert-ok` — The successful second sibling retains its output and has no error.
- erigon_development: **matches**, `verified-a/call-siblings-revert-ok` — The successful second sibling retains its output and has no error.
- erigon_development: **matches**, `verified-repeat/call-siblings-revert-ok` — The successful second sibling retains its output and has no error.
- erigon_release: **matches**, `verified-a/call-siblings-revert-ok` — The successful second sibling retains its output and has no error.
- erigon_release: **matches**, `verified-repeat/call-siblings-revert-ok` — The successful second sibling retains its output and has no error.
- nethermind_development: **matches**, `verified-a/call-siblings-revert-ok` — The successful second sibling retains its output and has no error.
- nethermind_development: **matches**, `verified-repeat/call-siblings-revert-ok` — The successful second sibling retains its output and has no error.
- nethermind_release: **matches**, `verified-a/call-siblings-revert-ok` — The successful second sibling retains its output and has no error.
- nethermind_release: **matches**, `verified-repeat/call-siblings-revert-ok` — The successful second sibling retains its output and has no error.
- reth_development: **matches**, `verified-a/call-siblings-revert-ok` — The successful second sibling retains its output and has no error.
- reth_development: **matches**, `verified-repeat/call-siblings-revert-ok` — The successful second sibling retains its output and has no error.
- reth_release: **matches**, `verified-a/call-siblings-revert-ok` — The successful second sibling retains its output and has no error.
- reth_release: **matches**, `verified-repeat/call-siblings-revert-ok` — The successful second sibling retains its output and has no error.
